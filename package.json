{
  "name": "herdr-process-name-split",
  "version": "1.0.0",
  "description": "Label each split border with its current path or foreground process.",
  "private": true,
  "engines": {
    "node": ">=22.18.0"
  },
  "type": "module",
  "scripts": {
    "check": "tsc",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [
    "herdr",
    "herdr-plugin",
    "terminal",
    "tab-names",
    "process"
  ],
  "author": "tbqq",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.0"
  }
}
